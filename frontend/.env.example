# Vercel deployment (mock mode) — add this one env var in the Vercel dashboard.
# Set to 'true' to use the mocked backend simulation.
# Set to 'false' (or omit) to proxy to a real backend API.
USE_MOCK=false
